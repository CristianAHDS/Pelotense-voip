{
  "name": "voip-project",
  "private": true,
  "scripts": {
    "server:dev": "cd server && npm run dev",
    "server:dev:jornal": "cd server && npm run dev",
    "server:build": "cd server && npm run build",
    "client:dev": "cd client && npm run dev",
    "client:dev:jornal": "set VITE_SERVER_HOST=192.168.8.94 && cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "client:tauri": "cd client && npm run tauri",
    "serve": "cd client && npm run build && cd ../server && npm run dev",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test",
    "e2e": "playwright test",
    "e2e:server": "npm run client:build && cd server && npm run build && set DB_PATH=./data/e2e.db&& npm start"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
